# Copyright (c) 2021 Nordic Semiconductor ASA
# SPDX-License-Identifier: Apache-2.0
#
# This file is the application Kconfig entry point. All application Kconfig
# options can be defined here or included via other application Kconfig files.
# You can browse these options using the west targets menuconfig (terminal) or
# guiconfig (GUI).

mainmenu "HealthyPi 5 Project Configuration"

config HEALTHYPI_USB_CDC_ENABLED
    bool "Enable USB CDC"
    default n
    help
      Enable USB CDC

config HEALTHYPI_DISPLAY_ENABLED
    bool "Enable Display"
    default n
    help
      Enable Display

config HEALTHYPI_USB_MSC_ENABLED
    bool "Enable USB Mass Storage"
    default n
    help
      Enable USB MSC

config HEALTHYPI_SD_CARD_ENABLED
    bool "Enable SD Card"
    default n
    help
      Enable SD Card

config HEALTHYPI_OP_MODE_DISPLAY
    bool "Enable Display Mode"
    default n
    help
      Enable Display Mode
      
source "Kconfig.zephyr"
